export default async function getItems() {
  // const nftData = await getNFTs();
  // const nftPromises = nftData.map(async (nft) => {
  //   const json = await fetch(nft.data.uri).then((response) => response.json());
  //   // console.info(json);
  //   return { ...nft, json };
  // });
  // // console.info(nftPromises.length);
  // const items = Promise.all(nftPromises);
  // return { items };
}
